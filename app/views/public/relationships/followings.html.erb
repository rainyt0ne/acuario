<div class="container py-5">
  <div class="row">

    <div class="col-md-2 text-center mt-5">
      <button type="button" class="btn btn-warning text-light rounded-circle p-0 top-btn", style="width:10rem;height:10rem;">
        <h2><strong>Followees</strong></h2>
      </button><br>
      <button type="button" class="btn btn-warning rounded-circle p-0 mt-2 mr-5 top-btn", style="width:3rem;height:3rem;"></button><br>
      <button type="button" class="btn btn-warning rounded-circle p-0 mt-2 ml-5 top-btn", style="width:1rem;height:1rem;"></button><br>
      <button type="button" class="btn btn-warning rounded-circle p-0 mt-2 mr-3 top-btn", style="width:2rem;height:2rem;"></button>
    </div>

    <div class="col-md-9 offset-md-1 py-4">
      <% if @user.followings.count > 0 %>
        <table class="table">
          <tr>
            <th></th>
            <th>name</th>
            <th>フォロー数</th>
            <th>フォロワー数</th>
          </tr>
          <% @users.each do |user| %>
          <tr>
            <td><%= image_tag user.get_profile_image(90,90), class: "profile_image" %></td>
            <td class="text-truncate" style="max-width: 300px;">
              <%= link_to user_path(user.id), class: "text-decoration-none text-info font-weight-bold" do %>
                <%= user.name %>
              <% end %>
            </td>
            <td><%= user.followings.count %></td>
            <td><%= user.followers.count %></td>
          </tr>
          <% end %>
        </table>
      <% else %>
        <p>あなたをフォローしているユーザーはまだいません。</p>
      <% end %>
      <div>
        <%= link_to '<< Back ', :back, class: "text-decoration-none text-info font-weight-bold" %>
        |
        <%= link_to ' Followers ', user_followers_path(@user), class: "text-decoration-none text-info font-weight-bold" %>
        |
        <%= link_to ' Posts >>', posts_path, class: "text-decoration-none text-info font-weight-bold" %>
      </div>
    </div>
  </div>
</div>